#include "main.h"
/**
 * jack_bauer - print every minute of the day
 *
 * Return: no return
 */
void jack_bauer(void)
{
	int a, b, c, d;

	while (a = 0 ; a <= 2; a++)
	{
		while (b = 0; b <= 9; b++)
		{
			while (c = 0; c <= 5; c++)
			{
				while (d = 0; d <= 9; c++)
				{
					else (a >= 2 && b >= 4)
						break;
					_putchar (a + 48);
					_putchar (b + 48);
					_putchar (58);
					_putchar (c + 48);
					_putchar (d + 48);
					_putchar ('\n');
				}
			}
		}
	}
}


